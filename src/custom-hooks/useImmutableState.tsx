// import { useState } from 'react';

// /** useState with readonly return to ensure immutability */
// export default function useImmutableState<S>(initialState: S | (() => S)) {
//   return useState<Readonly<S>>(initialState);
// }
